# הפעלת רב-משתמשים אמיתי — מדריך הקמה (10 דקות)

> ✅ **בוצע כבר** (2026-07-02): הפרויקט `pagia-baair` קיים, הסכמה הורצה (posts + chat_messages),
> והמפתחות ב-`config.js`. המדריך נשמר למקרה של הקמה מחדש מאפס.

כל הקוד כבר מוכן. נשארו לך 4 צעדים חד-פעמיים:

## צעד 1 — פתח פרויקט Supabase (חינם)
1. גש אל https://supabase.com ולחץ **Start your project** (הרשמה עם GitHub/Google).
2. לחץ **New project**, תן שם (למשל `pagia-baair`), בחר סיסמת DB חזקה (שמור אותה), Region: `eu-central` (הכי קרוב לישראל). לחץ **Create**.
3. המתן ~2 דקות עד שהפרויקט מוכן.

## צעד 2 — הרץ את הסכמה
1. בתפריט הצד של Supabase: **SQL Editor** → **New query**.
2. פתח את הקובץ `supabase-schema.sql` שבתיקיית הפרויקט, העתק את כל התוכן, הדבק והרץ (**Run**).
3. אמור להופיע "Success". זה יוצר את טבלת הפוסטים, הרשאות פתוחות, אחסון מדיה ו-Realtime.

## צעד 3 — העתק מפתחות
1. בתפריט הצד: **Settings → API**.
2. העתק את **Project URL** ואת **anon public** key.
3. פתח את `config.js` בתיקיית הפרויקט והדבק:
```js
const SUPABASE_URL      = 'https://xxxx.supabase.co';   // ה-Project URL שלך
const SUPABASE_ANON_KEY = 'eyJhbGciOi...';              // ה-anon key שלך
```

## צעד 4 — הפעל מחדש
`npm start` (או רענון). ב-Console אמור להופיע:
`[backend] connected — N posts loaded from DB`

## מה תקבל
- כל פוסט שמתפרסם נשמר ב-DB ונשאר גם אחרי סגירה.
- כל המשתמשים רואים את הפוסטים של כולם.
- **Realtime**: משתמש אחר מפרסם — הפוסט מופיע אצלך מיד עם התראה.
- תמונות/וידאו נשמרים ב-Storage ציבורי.
- בלי הרשמה — פתוח לכולם (RLS: קריאה+הוספה בלבד; אין מחיקה/עריכה פתוחה).

## בדיקה שזה עובד (רב-משתמשים)
פתח את האפליקציה בשני חלונות (או שני מחשבים), פרסם פוסט באחד — הוא יופיע בשני תוך שנייה.

## הערות
- בלי מילוי `config.js` האפליקציה ממשיכה לעבוד רגיל במצב דמו מקומי.
- ה-anon key בטוח לחשיפה בצד-לקוח (ההגנה היא ב-RLS).
- שלב הבא (כשתרצה): סנכרון צ'אט ותגובות ל-DB, לייקים אמיתיים, דיווח/חסימה נגד ספאם, ו-rate-limiting.
