# תוכנית Backend — רב-משתמשים אמיתי ("פתוח ליוזרים בלי הגבלה")

> ✅ **הוחלט ומומש** (R15-R17): נבחרה אפשרות 1 (Supabase) והיא רצה בפרודקשן.
> המסמך נשמר כתיעוד היסטורי של השיקולים.

## המצב היום
האפליקציה היא SPA בצד-לקוח בלבד. כל הנתונים (`pagiaPosts`, `stockPosts`, שיחות)
יושבים בזיכרון ונמחקים בכל רענון. אין שרת, אין בסיס נתונים, אין משתמשים אמיתיים —
מה שאתה רואה זה משתמש-דמו אחד (`ME`) עם נתוני דמו.

"רב-משתמשים אמיתי" (משתמשים שרואים זה את הפוסטים של זה) מחייב **שרת + בסיס נתונים**.
זה חורג מ-Iron Rule #6 ("בלי npm, דפדפן טהור") — כי backend הוא בהגדרה לא-דפדפן.
המטרה: לבחור מסלול ששומר את הלקוח נקי ככל האפשר.

## שלוש אפשרויות

**1. BaaS מנוהל — Supabase (מומלץ).**
Postgres + Auth + Realtime + Storage מנוהלים. הלקוח נשאר כמעט טהור: רק קריאות
`fetch`/SDK מעל HTTPS, בלי שלב build ובלי שרת לתחזק. תומך במשתמשים אנונימיים
(→ "פתוח בלי הגבלה"), עדכוני-פיד ושיחות ב-Realtime, ואחסון לתמונות מוצר אמיתיות.
Tier חינמי נדיב. מהיר להקמה (MVP ביום-יומיים).

**2. Node/Express + Postgres בעצמך.**
שליטה מלאה, אבל הרבה יותר עבודה: לכתוב API, לארח, לאבטח ולתחזק שרת משלך.
חורג יותר מ-Rule #6.

**3. פונקציות Serverless + DB מנוהל (Vercel/Netlify + Neon/Supabase).**
דרך-ביניים. פחות תחזוקה משרת מלא, יותר גמישות מ-BaaS טהור.

## המלצה: Supabase
הכי מהיר, Realtime מובנה (פיד ושיחות חיים), אחסון תמונות, תומך אנונימי+אופציונלי,
ונשאר קרוב ל"דפדפן טהור" (טוענים `supabase-js` מ-CDN, בלי npm-build).

### מודל נתונים מוצע
`users` · `posts` (טבלה אחת ל-pagia+stock עם עמודת `feed_type`) · `comments` ·
`saves` · `conversations` · `messages`. תמונות → Supabase Storage bucket.
Realtime subscriptions על `posts` ו-`messages`.

### שלבי מעבר
1. פתיחת פרויקט Supabase + הגדרת טבלאות ומדיניות RLS (קריאה פתוחה, הוספה מותרת).
2. החלפת המערכים בזיכרון (`pagiaPosts`/`stockPosts`) בקריאות ל-DB.
3. הוספת Realtime לפיד ולצ'אט.
4. העלאת תמונות ל-Storage במקום data-URL בזיכרון.
5. זהות: אנונימי כברירת-מחדל (פרסום בלי הרשמה) + התחברות אופציונלית (magic-link).

### נקודות לתשומת-לב ("פתוח בלי הגבלה")
פרסום פתוח בלי הרשמה מזמין ספאם והתעללות. לפני העלייה לאוויר צריך:
מנגנון דיווח/חסימה בסיסי, rate-limiting, וסינון תוכן. בנוסף — שמירת פרטים
אישיים מחייבת התייחסות לפרטיות (GDPR). ה-i18n נשאר בצד-לקוח, בלי תלות ב-backend.

## החלטות שצריך ממך לפני מימוש
- Supabase, שרת עצמאי, או Serverless?
- אירוח: היכן (Supabase cloud / Vercel / VPS)?
- פרסום אנונימי לגמרי, או הרשמה קלה (אימייל) לפני פרסום?
